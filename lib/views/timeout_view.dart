import 'package:flutter/material.dart';

class TimeoutView extends StatelessWidget {
  const TimeoutView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Timeout')),
    );
  }
}